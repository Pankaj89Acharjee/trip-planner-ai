# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Debug Firebase Secrets
        run: |
          echo "Checking Firebase secrets..."
          [ -z "${{ secrets.NEXT_PUBLIC_FIREBASE_API_KEY }}" ] && echo "❌ API_KEY is not set" || echo "✅ API_KEY is set"
          [ -z "${{ secrets.NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN }}" ] && echo "❌ AUTH_DOMAIN is not set" || echo "✅ AUTH_DOMAIN is set"
          [ -z "${{ secrets.NEXT_PUBLIC_FIREBASE_PROJECT_ID }}" ] && echo "❌ PROJECT_ID is not set" || echo "✅ PROJECT_ID is set"
          [ -z "${{ secrets.NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET }}" ] && echo "❌ STORAGE_BUCKET is not set" || echo "✅ STORAGE_BUCKET is set"
          [ -z "${{ secrets.NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID }}" ] && echo "❌ MESSAGING_SENDER_ID is not set" || echo "✅ MESSAGING_SENDER_ID is set"
          [ -z "${{ secrets.NEXT_PUBLIC_FIREBASE_APP_ID }}" ] && echo "❌ APP_ID is not set" || echo "✅ APP_ID is set"
      - name: Build Application
        run: |
          cd frontend-tourplanner          
          npm ci
          npm run build
        env:
          NEXT_PUBLIC_FIREBASE_API_KEY: ${{ secrets.NEXT_PUBLIC_FIREBASE_API_KEY }}
          NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN: ${{ secrets.NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN }}
          NEXT_PUBLIC_FIREBASE_PROJECT_ID: ${{ secrets.NEXT_PUBLIC_FIREBASE_PROJECT_ID }}
          NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET: ${{ secrets.NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET }}
          NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID }}
          NEXT_PUBLIC_FIREBASE_APP_ID: ${{ secrets.NEXT_PUBLIC_FIREBASE_APP_ID }}
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_HACKATHON_COMPETITION_2025 }}
          channelId: live
          projectId: hackathon-competition-2025
          entryPoint: firebase-backend

        #Firebase CLI needs webframeworks in Github Action to deploy NextJS Framework
        env:
          FIREBASE_CLI_EXPERIMENTS: webframeworks
          GOOGLE_APPLICATION_CREDENTIALS: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_HACKATHON_COMPETITION_2025 }}
          NEXT_PUBLIC_FIREBASE_API_KEY: ${{ secrets.NEXT_PUBLIC_FIREBASE_API_KEY }}
          NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN: ${{ secrets.NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN }}
          NEXT_PUBLIC_FIREBASE_PROJECT_ID: ${{ secrets.NEXT_PUBLIC_FIREBASE_PROJECT_ID }}
          NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET: ${{ secrets.NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET }}
          NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID }}
          NEXT_PUBLIC_FIREBASE_APP_ID: ${{ secrets.NEXT_PUBLIC_FIREBASE_APP_ID }}
