# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
on: pull_request
permissions:
  checks: write
  contents: read
  pull-requests: write
jobs:
  build_and_preview:
    if: ${{ github.event.pull_request.head.repo.full_name == github.repository }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Create .env file
      - run: |
          [ -z "$NEXT_PUBLIC_FIREBASE_API_KEY" ] && echo "API_KEY is not set" || echo "API_KEY is set"
          [ -z "$NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN" ] && echo "AUTH_DOMAIN is not set" || echo "AUTH_DOMAIN is set"
          [ -z "$NEXT_PUBLIC_FIREBASE_PROJECT_ID" ] && echo "PROJECT_ID is not set" || echo "PROJECT_ID is set"
          [ -z "$NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET" ] && echo "STORAGE_BUCKET is not set" || echo "STORAGE_BUCKET is set"
          [ -z "$NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID" ] && echo "MESSAGING_SENDER_ID is not set" || echo "MESSAGING_SENDER_ID is set"
          [ -z "$NEXT_PUBLIC_FIREBASE_APP_ID" ] && echo "APP_ID is not set" || echo "APP_ID is set"

        

          cd frontend-tourplanner
          npm ci
          npm run build
        env:
          NEXT_PUBLIC_FIREBASE_API_KEY: ${{ secrets.NEXT_PUBLIC_FIREBASE_API_KEY }}
          NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN: ${{ secrets.NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN }}
          NEXT_PUBLIC_FIREBASE_PROJECT_ID: ${{ secrets.NEXT_PUBLIC_FIREBASE_PROJECT_ID }}
          NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET: ${{ secrets.NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET }}
          NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID }}
          NEXT_PUBLIC_FIREBASE_APP_ID: ${{ secrets.NEXT_PUBLIC_FIREBASE_APP_ID }}
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_HACKATHON_COMPETITION_2025 }}
          projectId: hackathon-competition-2025
          entryPoint: firebase-backend

          #Firebase CLI needs webframeworks in Github Action to deploy NextJS Framework
        env:
          FIREBASE_CLI_EXPERIMENTS: webframeworks
          GOOGLE_APPLICATION_CREDENTIALS: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_HACKATHON_COMPETITION_2025 }}